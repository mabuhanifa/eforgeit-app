# Core Data Types

Explore the most common data types available in PostgreSQL.

- Numeric Types (`INTEGER`, `BIGINT`, `NUMERIC`)
- Character Types (`VARCHAR`, `TEXT`, `CHAR`)
- Date/Time Types (`TIMESTAMP`, `DATE`, `TIME`)
- Boolean Type
- The `SERIAL` Pseudo-Type
